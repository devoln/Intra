// Forwarder: the a/b panel generator moved into the debug module.
// Run from the repository root; the real code (and the optional browser panels)
// now live in intrasynth/web/debug.mjs.
await import("../../../intrasynth/web/debug.mjs");
